import StatusFilter from './StatusFilter';
import TodoSort from './TodoSort';

const TodoControls = () => {
  return (
    <div>
      <StatusFilter />
      <TodoSort />
    </div>
  );
};

export default TodoControls;
